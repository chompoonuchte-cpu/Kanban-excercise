## Agent skills

### Issue tracker

Issues live in GitHub Issues (`khthana/kanban-exercise`). See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the canonical five-label vocabulary with one override: `ready-for-human` → `HITL`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` at root and `docs/adr/`. See `docs/agents/domain.md`.
